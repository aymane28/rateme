{% extends 'base.html.twig' %}

{% block title %}Rate Me | Connexion
{% endblock %}

{% block body %}
	<div class="container mb-3 mt-5">
		<div class="row">
			<div class="col">
				<div class="d-flex justify-content-between">
					<a class="text-dark text-decoration-none text-color-gradient" href="{{ path('profiles') }}">
						<h6 class="text-color-gradient">
							<i class="bi bi-arrow-left"></i>
							Revenir à la liste des profils<h6></a>
						</div>
					</div>
				</div>
				<div class="row d-flex text-center justify-content-center align-items-center mb-2">
					<div class="col col-md-9 col-lg-7 col-xl-4">
						<p>
							<small>Bienvenue de nouveau sur la scène digitale! Connectez-vous pour découvrir les dernières évaluations, les commentaires de vos pairs et pour continuer à briller sur nos plateformes sociales. Votre présence est attendue avec impatience.</small>
						</p>
					</div>
				</div>
				<div class="row d-flex justify-content-center align-items-center h-100 my-auto">
					<div class="col col-md-9 col-lg-7 col-xl-4">
						<form action="{{ path('app_login') }}" method="post">
							{% if error %}
								<div class="alert alert-danger" role="alert">
									Email ou mot de passe incorrect
								</div>
							{% endif %}
							<div class="form-outline mb-4">
								<label class="form-label" for="username">Email</label>
								<input type="email" id="username" name="_username" class="form-control" value="{{ last_username }}" required>
							</div>
							<div class="form-outline mb-2">
								<label class="form-label" for="password">Mot de passe</label>
								<input type="password" id="password" name="_password" class="form-control" required/>
							</div>
							<!-- 2 column grid layout for inline styling -->
							<div class="row mb-4 d-flex text-end">
								<div
									class="col">
									<!-- Simple link -->
									<a class="text-secondary text-end" href="#!">
										<small>Mot de passe oublié ?</small>
									</a>
								</div>
							</div>
							{# If you want to control the URL the user is redirected to on success
																																																																																        <input type="hidden" name="_target_path" value="/account"> #}

							<!-- Submit button -->
							<div class="d-grid ">
								<button type="submit" class="btn btn-dark rounded-pill btn-block mb-4">Connexion</button>
							</div>

							<!-- Register buttons -->
							<div class="text-center">
								<p>Pas encore membre ?
									<a class="h6 text-dark text-decoration-none" href="{{ path('app_register') }}">S'inscrire</a>
								</p>
							</div>

						</form>
					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
